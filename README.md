# learning_module_bundlers

- This project assumes you are using pnpm as your package manager.

# Rollup

Watch Mode
```bash
pnpm rollup -c --watch
```

Build Mode
```bash
pnpm rollup -c <defaults to rollup.config.js>
```